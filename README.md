# Agriculture Service 

This is a Java, SpringBoot3 application using H2 database for deriving insights from raw data of Agriculture Crop Production. 
It has some APIs which returns data is different format and segregates it using states, by year and by crop. It also takes care of the conversions of the units as the data is not uniformed.
